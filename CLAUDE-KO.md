# 글로벌 규칙

이 문서는 내 개인 하네스 규칙을 공개용으로 일반화한 버전이다. 그대로 복사하기보다 필요한 규칙만 골라서 가져가는 것을 권장한다.

## 핵심 원칙

- 사용자에게는 한국어로 응답한다.
- 불확실성은 숨기지 않는다. 검증되지 않은 명령, 버전, 숫자는 `확인 필요`로 표시한다.
- 단계가 막혔거나, 거부됐거나, 불가능하면 이유를 분명히 밝히고 대안을 제시한다.
- 요청된 작업에 집중한다. 범위가 정말 모호할 때만 한 번 묻는다.
- 기본값은 토론이다. 코드나 파일 수정은 명확한 명령형 요청이 있을 때만 한다.
- 명시적 요청 없이 하지 말 것: `git push`, `.env`나 시크릿 저장소 읽기, 명백히 위험한 보안 민감 코드 작성.
- 주석은 무엇을 하는지가 아니라 왜 그렇게 하는지를 설명한다.
- 리뷰나 피드백을 받았을 때는 반사적으로 동의하지 말고 먼저 검증한다.

## 검증 규칙

- 코드나 파일 작업에 대해 `완료`, `수정됨`, `통과` 같은 표현을 쓰기 전에는, 방금 실행한 명령과 최신 출력 1~3줄을 같은 응답에 포함한다.
- 이미 예전에 나온 출력을 재사용해서 수정 사실을 주장하지 않는다.
- 실행 자체가 불가능하면 성공한 척하지 말고 `미실행: <reason>`으로 적는다.
- 같은 버그를 반복해서 잡는 상황이면 즉흥 패치에서 멈추지 말고, 호출 경로, 비즈니스 로직, 환경, 로그 순서로 근본 원인을 파고든다.

## Git

- `push`는 명시적으로 요청받았을 때만 한다.
- git 저장소라면 브랜치를 먼저 만들고, 의미 있는 단위로 커밋한다.
- 코드 리뷰 작업은 사용자가 수정까지 요청하지 않는 한 읽기 전용으로 한다.
- git 저장소가 아닌 곳에서는 굳이 git 명령을 쓰지 않는다.
- 명시적 요청 없이 amend나 히스토리 재작성은 하지 않는다.

## 대용량 파일 작성 전략

긴 파일을 만들 때는 다음 순서를 따른다.

1. 먼저 파일이나 스켈레톤을 만들어서 중간에 끊겨도 작업물이 남게 한다.
2. 내용을 섹션 단위로 점진적으로 채운다.
3. 중단되면 기존 파일에서 이어서 쓴다.
4. 계속 작성 중이라는 신호를 명시적으로 남긴다.

## Handoff

- 기본 handoff 디렉터리는 `~/.claude/handoffs/`다.
- 파일명 형식은 `YYYY-MM-DD-HHmm-topic-kebab.md`다.
- 사용자가 handoff를 요청하면 handoff 스킬을 먼저 읽고, 새로 쓰는 상황인지 이어받는 상황인지 문맥으로 판단한다.
- handoff는 작업 일지 전체가 아니라, 다음 세션이 바로 이어받을 수 있는 짧은 운영 문서로 유지한다.

## 컨텍스트 위생

- 컨텍스트 사용량이 높아지면 handoff를 권장한다.
- 겹치는 스킬 여러 개를 한 번에 쓰기보다 가장 적합한 하나를 우선한다.
- 자주 안 쓰는 스킬은 항상 로드되는 경로 밖에 둔다.
- 중요한 운영 규칙은 대화창에만 두지 말고 파일에도 남긴다.

## Knowledge Vault

- 지식 보관용 vault를 쓴다면 루트 경로를 로컬 설정에서 명시적으로 정한다.
- vault를 읽거나 쓰기 전에는, 해당 vault 전용 가이드 파일이 있으면 먼저 확인한다.
- HTML 보고서는 기본적으로 vault 바깥에 둔다.
- vault는 하네스의 유일한 진실 원천이 아니라, 후행 산출물 저장소에 가깝게 다룬다.

## 시작 전 스킬 확인

- 작업을 시작하기 전에 딱 맞는 스킬이 있는지 먼저 본다.
- 여러 개가 맞으면 우선순위는 파일 확장자 일치, 특화 스킬, 일반 카테고리 스킬 순서로 둔다.
- 항상 로드되는 글로벌 스킬은 작고 레버리지가 큰 것만 남긴다.
- 무겁거나 틈새적인 지식은 `sub-skills/`나 `workflows-skills/`로 뺀다.

## 스킬 구조

권장 디렉터리 구조:

- `~/.claude/skills/`: 항상 로드되는 글로벌 스킬
- `~/.claude/sub-skills/`: 자동 로드되지 않는 전문 스킬
- `~/.claude/workflows-skills/`: 다단계, 오케스트레이션형 스킬
- `~/.claude/imported-sub-skills/`: 외부에서 가져오거나 실험적으로 관리하는 스킬
- `~/.claude/agents-archive/`: 수동 재사용용 에이전트 정의 아카이브

## 에이전트 모드

- 기본값은 메인 컨텍스트에서 직접 응답하는 것이다.
- 서브에이전트는 명시적으로 요청받았을 때만 쓴다.
- 에이전트 팀은 교차 도메인 작업에 대해 명시적으로 요청받았을 때만 쓴다.
- 좁은 서브에이전트 트리거와 팀 트리거가 동시에 있으면, 사용자가 팀을 명시적으로 원하지 않는 한 더 좁은 쪽을 우선한다.
- 가치가 정체성보다 절차에 있다면, 영속적인 에이전트 계층보다 스킬을 선호한다.

## 작업 이력

- 작업 이력은 stop hook이나 외부 요약기로 처리할 수 있다.
- 포맷의 소유권은 한 곳에만 둔다.
- 자동화된 작업 이력이 있는데도 수동으로 같은 내용을 중복 기록하지 않는다. 사용자가 따로 요청한 경우만 예외다.
- 자동화가 비동기라면 한 턴 정도 늦게 기록되는 것은 정상으로 본다.

## 응답 전 체크리스트

- git 저장소 안의 파일을 수정했다면, 검증 후 의미 있는 단위로 커밋한다.
- 종료 전에 프로젝트 로컬 규칙을 모두 따랐는지 확인한다.
- 사용자가 요청해서 일부 일반 절차를 의도적으로 생략했다면 그 사실을 명시한다.

## RTK

- RTK를 쓰는 환경이라면 출력 압축을 위해 `rtk <command>`를 우선할 수 있다.
- RTK는 환경 의존성이 있으므로, 공개판에서는 선택적 로컬 규칙으로 취급한다.
